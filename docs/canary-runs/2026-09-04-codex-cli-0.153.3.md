# Canary run, 2026-09-04, codex-cli 0.151.0 -> 0.153.3

Trigger: pin bump approved by Scott ("Approved, upgrade the CLI and rerun canaries
once phase 1 finishes") to reach gpt-6-astra. `config/local.json` `allowed_versions`
for codex moved to `codex-cli 0.153.3`; the configured executable (nvm prefix) was
upgraded in the same pass as the `~/.local` npm install.

Command: `canaries/run_canaries.py --direction both --out <json>`; raw rows in the
sibling .json, console in the sibling .log.

Result:

- claude->codex: 19 calls graded, 19/19 contract-valid, 19/19 first-attempt,
  p50 29.3s / p95 55.3s, continuations 6/6, schema-pressure complete/ok.
- timeout canaries, both directions: timed_out, orphans 0, ok.
- codex->claude: BLOCKED, peer_auth_failure. `claude auth status` for the
  configured executable reports loggedIn false. No live calls were made in this
  direction, so nothing about it is verified at 0.153.3. Re-run
  `--direction codex-to-claude` after the Claude CLI is authenticated.

Verdict: the direction this machine uses (Claude consulting Codex through the
codex-peer MCP) is version-bound PASS at 0.153.3. The reverse direction carries
no evidence at this version. The runner printed FAIL overall because of the
blocked direction, not because of any failed call.

Offline suite at the new pin: tests/test_suite.py 504 passed, 0 failed.
